<VirtualHost *:80>
    DocumentRoot /app

    <Directory "/app">
        AllowOverride all
        Require all granted
        SetEnvIfNoCase Origin "https?://(www\.)?(localhost|veglab)(:\d+)?$" AccessControlAllowOrigin=$0
        Header always set Access-Control-Allow-Origin %{AccessControlAllowOrigin}e env=AccessControlAllowOrigin
        #Header always set Access-Control-Allow-Origin "http://localhost:4200"
        #Header always set Access-Control-Allow-Origin "http://veglab"
        Header always set Access-Control-Allow-Credentials "true"
        Header always set Access-Control-Allow-Headers: "X-HTTP-Method-Override, Content-Type, origin, authorization, x-requested-with"

        RewriteEngine On
        RewriteCond %{REQUEST_METHOD} OPTIONS
        RewriteRule ^(.*)$ $1 [R=200,L]
    </Directory>
</VirtualHost>